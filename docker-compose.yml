services:
  hr-assistant:
    build: .
    ports: ["8000:8000"]
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    env_file:
      - .env
    volumes:
      - hcm-data:/app/data
    restart: unless-stopped
volumes:
  hcm-data:
